package com.openclaw.autoreply.reply;

/**
 * Exec directive facade — re-exports from exec sub-package.
 * Mirrors {@code auto-reply/reply/exec.ts}.
 *
 * <p>
 * In Java callers should use the exec sub-package directly.
 * </p>
 */
public final class Exec {
    private Exec() {
    }

    // Delegates to exec.ExecDirective.extractExecDirective — full integration
    // deferred.
}
